window.addEventListener('load', function() {
  var view = document.querySelector("body");
  view.innerHTML = JSON.stringify(translate(oldTemplates[0].data));
})

function translate(template) {
  var buffer = {};
  
  return buffer;
}